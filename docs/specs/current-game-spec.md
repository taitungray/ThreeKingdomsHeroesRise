# 三國：群英再起目前遊戲規格

狀態：CURRENT。更新日期：2026-08-30。本文件只描述目前可由程式、資料、manifest 或測試核對的事實；規範、待辦、問題細節與歷史方案各自放在對應目錄。

## 產品與核心循環

- `已確認`：直式 Web/H5 三國放置 RPG。
- `已確認`：核心循環為訪客／登入 → 配置武將與裝備 → 自動戰鬥 → 三個普通波次 → Boss → 結算資源與關卡 → 成長後再戰。
- `已確認`：資料目前包含 50 名武將、20 章／100 關、普通敵人池、每波敵將與 Boss 敵將。
- `已確認`：50 名武將目前主要共用 12 組 `visual` 行為模板；完整 `skillSpecs` 只有 12 組，其餘角色詳情使用通用 fallback。
- `已確認`：關卡名稱由循環前綴產生，並非逐關依章節／敵人策劃；章名與首關名存在語意錯配。

## 主畫面與操作

- 主公牌與小尺寸武將頭像保留完整肖像，不以 cover 裁切頭盔、鬍鬚或肩線；source／www UI 幾何測試均通過。
- `已確認`：50 張 `portrait-*.webp` 均為 64×64 WebP；UI 頭像依用途採正方形尺寸契約：HUD／敵將預告／對話／編隊 40px、武將名冊 48px、詳情／紙娃娃展示 88px、列傳 54px、演武列 30px；360px 以下 HUD 與展示尺寸降為 36px／78px。
- `已確認`：`portrait-zhangfei-v1.webp` 已換成與張飛 v4 全身戰鬥素材同身分的黑鬍、紅巾、黑金甲像素肖像；初始對話 DOM 也直接指向該資產。固定尺寸美術質感仍需人工截圖／實機確認。
- `已確認`：遊戲內建 jf open 粉圓 2.1 為 `Huninn Game`，DOM 與 Canvas 統一使用同一套繁中字模，移除執行期對系統楷體／等寬英文字型的依賴；數字使用等寬數字特性，標題靠尺寸與字距建立層級。字型檔與 OFL-1.1 授權一併位於 `assets/fonts/`。
- `已確認`：長文字有獨立欄位與省略邊界，切換命令面板會重設共用內容捲動位置。source `test:ui` 已抽查劉備、張飛、諸葛亮、貂蟬、呂布詳情。
- `已確認`：UI 採「暗金鐵木軍帳」App 級材質重設計（`css/base.css`／`hud.css`／`panels.css`／`css/ui-redesign.css`／`css/ui-overhaul.css`）：朱砂印鈕、兩列頂部指揮條、鐵木指揮甲板、紙卷面板與軍府登入殼；`npm test`／`test:ui` 通過。仍缺基準尺寸截圖與實機證據關閉 VERIFY。
- `已確認`：UI 以手機直式為唯一主版面：390px authored viewport、面板保留安全邊距、武將／編隊名冊手機採 2 欄（360px 以下降為 1 欄），名冊頭像維持 48px 正方形 token；桌面與觸控平板預覽不展開遊戲殼，仍維持 390px 直式世界與原有輸入座標契約。
- `已確認`：武將名冊固定先顯示出戰角色，再顯示其他已擁有角色，最後顯示未相遇角色；每一層內仍依玩家選擇的戰力／等級／兵種排序。
- `已確認`：頂欄主公牌點一次開啟「主公軍府」；經驗分數疊在經驗條上單行顯示，不再與等級或稱號疊字。
- `已確認`：主公軍府含肖像／稱號／頭像框裝備、經驗條與不可點擊的進度摘要；核心功能統一由底部導覽或右側軍務抽屜進入，不在軍府重複放導覽捷徑。
- `已確認`：遊戲根節點禁止瀏覽器原生文字選取、右鍵內容選單與圖片／Canvas 拖出保存；可編輯輸入欄保留文字選取例外，遊戲內按鈕不受影響。規範與參考案一致，source `test:ui` 會驗證 `selectstart`、`contextmenu` 與 `dragstart` 攔截。
- `已確認`：右側常駐最多 4 快捷（設定／郵件／日務／更多）＋軍務抽屜；抽屜含行商、活動、圖鑑、成就、列傳、演武、問天樓、副本。
- `已確認`：底欄「征戰」會關閉面板回到戰場；敵將預告顯示上限 2 秒。
- `已確認`：勝敗結算使用紙卷戰報彈窗（繁中文案），不再略過為僅 toast／自動推關，也不使用英文 ribbon。
- `已確認`：戰鬥支援自動進行、速度切換、波次、Boss、勝敗結算與重試／下一關路徑。
- `已確認`：專案目前沒有 `navigator.vibrate` 呼叫。
- `已確認`：技能觸發會呼叫大型底部對話；自動推關進入 `startStage()` 時會關閉目前面板。
- `已確認`：訪客設定頁目前讀取不存在的 `username` 欄位，實際資料使用 `displayName`。

## Runtime 與戰鬥渲染

- `已確認`：載入順序為 audio → data（game-data, hero-biographies, battle-quotes, shop-data）→ core → combat → render → UI（base, heroes, modes, panels）→ cloud-save → main；`game.js` 只保留相容性標記。
- `已確認`：CSS 採模組化拆分架構（`css/base.css`、`css/hud.css`、`css/avatars.css`、`css/panels.css`），由 `styles.css` 統一匯總，兼顧開發維護性與發布建置效能。
- `已確認`：單位狀態包含移動、攻擊 action、受擊、死亡、技能、波次與結算資料。
- `已確認`：單位 HUD 血條與技能氣條分列：血條在上、氣條在下，各佔獨立 Y；`drawHealthBar`／`drawSkillEnergyBar` 各只宣告一次，禁止同條紅藍併色。
- `已確認`：角色 body、portrait、attack、mount、Boss、VFX、terrain 與 combat weapon 有 WebP／manifest 管理，Canvas 角色圖使用 nearest-neighbor。
- `已確認`：Canvas transform 洩漏已修正，source 與同步後 `www` 的真實 Chrome transform gate 通過。
- `已確認`：`attack-manifest.json` 為 version 6；59 張 64px `v2` 圖集保留作舊資產追蹤，runtime 以 59 張 96px `v3` 高細節全身圖集為基線（覆蓋全 50 名武將、5 類敵人士兵與 4 名首領 Boss），並對首四將（劉備、關羽、張飛、趙雲）改用 128px `v4` 試製圖集。每張維持 8 欄方向契約與 anticipation／windup／contact／follow-through／recovery 五列階段，renderer 依 `attackFrame` 實際抽格。
- `已確認`：`v3` 動作圖以 11 名名將（含呂布／諸葛亮／貂蟬專屬母圖）、3 種普通敵軍與 2 名 Boss 獨立母圖為基礎，全 50 名武將與其餘敵首依 `visual` 或兵種（步→關羽、騎→趙雲、弓→黃忠、謀→曹操）及董卓身分全面映射產出獨立 96px attack 與 move 實體檔案。核心 11 將不得互為像素複本；其餘武將仍可能共用 archetype 剪影。身分 alias 契約持久化於 manifests，舊肖像／半身 combat body 不再作正常戰鬥路徑；不可宣稱全 50 名武將已完成唯一專屬外觀。
- `已確認`：`move-manifest.json` version 3 管理全 59 個戰鬥單位的 4 幀 96px `v3` 移動條帶；首四將另用 128px `v4` 移動條帶（左腳接地／重心通過／右腳接地／回復通過）。條帶與攻擊母圖皆朝右，runtime 只依 `unit.facing` 鏡像；v3 產生器 flop set 為空。只有 `unit.moving` 時抽格，攻擊會中斷移動循環，停下改回 idle；死亡狀態（`unit.dead`）由動作圖接續播放後仰、旋轉與 squash 淡出，消除死亡瞬間憑空消失缺陷。
- `已確認`：`npm run generate:combat-actions` 先由核心／支援／敵軍 v3 母圖加上呂布／諸葛亮／貂蟬專屬 strip 建立 59 張 96px 攻擊圖集與移動條帶，再由兩張 v4 首四將母圖重建 4 張 128px attack／move 試製資產；`npm run test:combat-assets` 驗證全量 59 張動作圖與移動條帶的尺寸、alpha、cell coverage、外緣髒色帶、路徑、階段／步態指紋，以及核心 11 將不得互為像素複本。
- `已確認`：目前 ImageGen 提示、八張 master sheet 格位、背景去除、96px／128px 正規化、命名、alias、Canvas 消費與驗收流程已集中記錄於 [戰鬥人物圖片製作方式與規格](../standards/combat-character-asset-production.md)。
- `已確認`：7 個 Boss 身分目前映射到 4 張 Boss sprite；普通敵人、敵將與 Boss 大量使用其他武將 body alias。
- `已確認`：戰場兵器有 9 個基礎資產，額外武器 ID 大量 alias 到通用資產，握點主要依武器類型共用而非逐角色／方向定義。
- `已確認`：核准的 `v3` 動作圖已內嵌手部相連兵器，renderer 不再疊加第二把外部兵器；程序 fallback 才使用獨立 combat weapon。
- `已確認`：renderer 仍保留程序 body／mount fallback；依現行戰鬥渲染契約不可作為正式資產。
- `不一致`：UI 動作規範禁止全畫面震動，但 runtime 仍保留 shake 路徑。

## 成長、內容與面板

- `已確認`：編隊支援 3×3 換位操作與前後排站位加成（前排堅壁、中排突擊、後排策應），並即時計算啟動之武將羈絆。
- `已確認`：戰法改為每場攜帶 1 個出戰生效，支援策略性切換與升級。
- `已確認`：裝備以玩家擁有清單為限進行換裝與比較；問天樓、演武場與日常副本全部接通正式戰鬥與結算流程。
- `已確認`：戰令（征戰敕令）具備 1~30 階等級晉升與獎勵領取系統；全週期經濟數值模擬腳本（`npm run test:economy`）驗證 1~100 關產銷平衡。
- `已確認`：離線掛機（AFK）具備專屬征戰回歸結算面板，支援名將碎片、玉璧與資源一鍵/雙倍入庫；戰鬥結算具備 MVP 傷害高亮標記。
- `已確認`：戰鬥支援名將怒氣大招立繪奧義特寫（Skill Cut-in）、暴擊與「斬/破敵」打擊浮字、頂部動態主線目標指引（Quest Tracker）與武將詳情緣分圖譜。
- `已確認`：戰鬥全面接入實體 WAV 音效：無雙大招觸發專屬奧義音效、暴擊觸發加重打擊音效、Boss 破敵觸發震撼音效。
- `已確認`：圖鑑實裝四大勢力（蜀漢/曹魏/東吳/群雄）名將收集共鳴里程碑（3/6/10 人）與玉璧獎勵；戰鬥支援名將與 Boss 專屬動態像素喊話氣泡。
- `已確認`：武將詳情為養成頁：身份／四維／戰法／裝備／緣分 chip 在可捲區，列傳預設摺疊，神兵真名合進裝備列；升級／升星／戰法／出戰釘在面板底列，精煉與突破收在進階。全 50 名將列傳資料仍在摺疊內。
- `已確認`：實裝四大兵種動態相剋機制（步克弓、弓克騎、騎克步、謀士法攻）與「兵法精研」（Lv.1~10）屬性養成；戰鬥畫面支援 20 大章動態戰場天氣環境粒子（細雨、飛雪、赤壁烈火微粒、黃沙風暴、秋風落葉與天命金光）。
- `已確認`：實裝名將「專屬神兵真名共鳴」系統（關羽·真青龍偃月、張飛·真丈八蛇矛、趙雲·真龍膽亮銀、諸葛亮·真羽扇綸巾、呂布·真方天畫戟、劉備·真雌雄雙股等），裝備專屬神兵即時啟動獨立特權與金色光輝標籤。
- `已確認`：實裝「名將傳奇列傳試煉」（Hero Fate Trials，關羽·千里走單騎、趙雲·長坂坡百人斬、諸葛亮·七星壇借東風、曹操·官渡奇襲烏巢、呂布·虎牢關戰三英、周瑜·赤壁烈火燎原），獨立戰鬥模式，通關解鎖專屬動態頭像框、稱號、大量名將碎片與玉璧。
- `已確認`：實裝戰鬥刀劍斬擊流光殘影（Weapon Slash Trails）、金屬重防格擋火星（Clash Sparks）、精準暴擊 Hit Stop 頓挫微震，以及箭矢法術飛行角度旋轉與破空風痕尾跡。
- `已確認`：實裝武將長按快速連續升級（Hold-to-Rapid-Level-Up）、一鍵最佳神裝推薦穿戴（One-Click Auto-Equip）與戰力即時浮動反饋。

## 存檔、離線與雲端

- `已確認`：本地存檔 schema version 為 3，載入支援 v2／v3 並補入預設欄位。
- `已確認`：存檔包含資源、關卡、武將、編隊、裝備、任務、活動、商城與離線狀態；`persist()` 先寫 localStorage，再排入雲端同步。
- `已確認`：正式 Firebase Web 配置與 Android `google-services.json`（projectId: `rayon-819bc`）已接入，支援 Google Auth 登入與 Firestore 雲端存檔同步。

## 目前品質判定

目前判定：`FAIL／不可發布`。

主要事實：96px `v3` 基線與首四將 128px `v4` 戰鬥試製已接入 runtime；命令列資產 gate 會阻止外緣矩形髒色帶，390×720 source browser 畫面已確認四名試製角色無矩形髒底。但全 50 名唯一外觀、五階段、八方向、握點、其餘基準尺寸與完整生命週期仍缺完整瀏覽器逐狀態畫面證據；其餘 UI、流程、內容與正式平台條件仍依問題清單及 QA 矩陣驗收，不得以資產檔存在宣稱可發布。

整體收斂方向見 [全遊戲調整計畫](../work/game-adjustment-plan.md)，詳細缺陷見 [已知問題](../issues/known-issues.md)，行動順序見 [目前工作清單](../work/active-backlog.md)，發布門檻見 [QA 測試矩陣](../qa/qa-test-matrix.md)。

## 維護方式

玩法、資料契約、存檔 schema、主要 UI 結構或 runtime 邊界改變時更新本檔。不要在本檔排工作或記錄長篇除錯；規範改到 `standards/`，待辦改到 `work/`，問題改到 `issues/`。
